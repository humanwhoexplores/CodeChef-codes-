#include<stdio.h>
# define mod 1000000007
int main()
{
	int T,i,max,sum,count,array[100000];scanf("%d",&T);
	
	while(T--)
	{    count=0;sum=0
		int N;scanf("%d",&N);
		for(i=0;i<N;i++)
		    scanf("%d",&array[i]);
		    
		   max=array[0];
		   for(i=1;i<N;i++)
		   {
		   	if(max<array[i])
              {
              	max=array[i];
              	
              	
              }

		   } 
		   for(i=0;i<N;i++)
		   {
		   	if(array[i]==max)
		   	   count++;
		   }
		    sum+=1;
		for(i=1;i<=)
		
		
	}
	
	
}
